# Example environment variables
# MongoDB connection string used by the API routes
# Replace with your connection string before running the app
MONGODB_URI=mongodb+srv://<user>:<password>@cluster0.example.mongodb.net/mydb?retryWrites=true&w=majority

# JWT secret used to sign session tokens. Set this to a long random string in production.
JWT_SECRET=changeme_replace_with_a_strong_secret

# Optional: control SameSite for session cookie (lax|strict|none). Default: lax
# SESSION_SAMESITE=lax
